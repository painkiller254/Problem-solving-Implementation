def angryProfessor(k, a):
    # Write your code here
    count = 0
    
    for i in a:
        if i <= 0:
            count += 1
        if count >= k:
            cancelled = False
        else:
            cancelled = True
    if cancelled:
        return "YES"
    else:
        return "NO"
