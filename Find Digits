def findDigits(n):
    # Write your code here
    count = 0
    
    res = [int(x) for x in str(n)]
    for i in res:
        if i != 0 and n % i == 0:
            count += 1
            
        
            
    return count
